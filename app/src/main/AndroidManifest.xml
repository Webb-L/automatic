<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="top.webb_l.automatic">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.AutoAccessibility">
        <activity
            android:name=".acitivity.OpenWebActivity"
            android:label="@string/openWeb"
            android:screenOrientation="portrait" />
        <activity
            android:name=".acitivity.UseScriptActivity"
            android:label="@string/use_script"
            android:screenOrientation="portrait" />
        <activity
            android:name=".acitivity.EditAutoMaticActivity"
            android:label="@string/edit_script"
            android:screenOrientation="portrait" />
        <activity
            android:name=".acitivity.SelectPackageActivity"
            android:label="@string/select_package"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AutoAccessibility.NoActionBar" />
        <activity
            android:name=".acitivity.AddStepActivity"
            android:label="@string/add_step"
            android:screenOrientation="portrait" />

        <service
            android:name=".service.AutoAccessibilityService"
            android:enabled="true"
            android:exported="true"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/auto_accessibility_service_config" />
        </service>

        <activity
            android:name=".acitivity.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>